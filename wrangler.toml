name = "ssq-cloudflare"
pages_build_output_dir = "."
compatibility_date = "2024-09-23"

# 可选：绑定 KV 后 /api/refresh 抓取的数据会持久化，后续 /api/trend、/api/generate 优先使用。
# 未绑定 KV 时刷新接口仍返回本次抓取结果，但不会持久化（静态样本不变）。
[[kv_namespaces]]
binding = "SSQ_DATA"
id = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID"
